#ifndef RTIOW_SCENES_SCENES_H
#define RTIOW_SCENES_SCENES_H

#include <stdint.h>

void three_spheres(uint8_t * const p_buffer);
void wide_angle(uint8_t * const p_buffer);
void final_scene(uint8_t * const p_buffer);

#endif /* RTIOW_SCENES_SCENES_H */
